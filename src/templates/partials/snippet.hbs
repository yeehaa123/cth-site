<section class="snippet">
  <div class='page-header'>
    <a data-toggle='collapse' href='#snippet{{index}}'><h1>{{data.title}}</h1></a>
  </div>
  <div class="collapse" id='snippet{{index}}'>
    {{#if data.topImage}}
        <img src="{{data.topImage}}"></img>
    {{/if}}
    {{#if data.svg}}
      {{> hammer }}
    {{/if}}
    {{#unless data.empty}}
      <section class='hyphenate article'>
        {{#markdown}}
          {{{page}}}
        {{/markdown}}
      </section>
    {{/unless}}
    {{#if data.bottomImage}}
        <img src="{{data.bottomImage}}"></img>
    {{/if}}
  </div>
</section>
